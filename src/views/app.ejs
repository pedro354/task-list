<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body class="bg-dark text-light">
    <%- include('partials/header')  %>
    <div class="container mt-5">
        <h1>Lista de Tarefas</h1>
        <ul class="list-group">
            <!-- chamando tasks(as tarefas), que ta vindo do tasksController. Chamando um forEach e para cada task(tarefa) eu vou criar um li que tem um "a" que tem um link que vai levar para uma url  que foi mapeado no taskController, accesando a pagina individual de uma tarefa  -->
            <% taskLists.forEach(taskList=> { %>
                <li class="list-group-item bg-dark d-flex flex-column align-center py-3" >
                    <article class="task d-flex flex-column">
                        <!-- link leva para uma página especifica do id -->
                        <a href="/app/<%= taskList.id%>" class="task-title d-flex justify-content-between align-items-center text-decoration-none text-light">
                            <h2 class="fs-3 fw-light">
                                <%= taskList.title%>
                            </h2>
                            <!-- um formulario para obter o metodo post -->
                            <form id="deleteList" action="/app/<%= taskList.id  %>/excluir" method="post">
                                <button type="submit" class="btn btn-danger btn-md">Excluir</button>
                            </form>
                        </a>
                    </article>
                </li>
                <% }) %>
            </ul>   
            <a href="/app/nova-lista" class="btn btn-success mt-3 btn-lg">Nova Lista de Tarefas</a>
            </div>
            <%- include('partials/footer')  %>
            
            <script src="/assets/script.js"></script>
        </div>
</body>

</html>